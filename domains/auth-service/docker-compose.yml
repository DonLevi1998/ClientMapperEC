services:
  auth-hash:
    build:
      context: ./auth-hash
      dockerfile: Dockerfile
    container_name: auth-hash
    ports:
      - "5031:5031"
    env_file:
      - ./auth-hash/.env

  auth-login:
    build:
      context: ./auth-login
      dockerfile: Dockerfile
    container_name: auth-login
    ports:
      - "5030:5030"
    env_file:
      - ./auth-login/.env

  auth-rol:
    build:
      context: ./auth-rol
      dockerfile: Dockerfile
    container_name: auth-rol
    ports:
      - "5032:5032"
    env_file:
      - ./auth-rol/.env

  auth-token:
    build:
      context: ./auth-token
      dockerfile: Dockerfile
    container_name: auth-token
    ports:
      - "5033:5033"
    env_file:
      - ./auth-token/.env
